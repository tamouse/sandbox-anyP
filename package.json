{
  "name": "anyp-function",
  "version": "1.0.0",
  "description": "An example of an `anyP` function written with lodash reduce. Using it to find out if a particular item matches a regexp in a collection.",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "lodash": "4.17.10"
  },
  "devDependencies": {
    "parcel-bundler": "^1.6.1"
  },
  "keywords": []
}